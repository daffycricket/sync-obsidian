# =============================================================================
# SyncObsidian - Configuration
# =============================================================================
# Copier ce fichier vers .env et adapter les valeurs :
#   cp .env.example .env
#
# =============================================================================

# -----------------------------------------------------------------------------
# API SyncObsidian
# -----------------------------------------------------------------------------

# Clé secrète pour signer les tokens JWT
# IMPORTANT : Générer une clé unique en production !
# Commande : python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-this-secret-key-in-production

# Base de données SQLite (optionnel, valeur par défaut ci-dessous)
# DATABASE_URL=sqlite+aiosqlite:///./data/syncobsidian.db

# Chemin de stockage des fichiers (optionnel, valeur par défaut ci-dessous)
# STORAGE_PATH=./data/storage

# Durée de validité des tokens en minutes (optionnel, défaut: 1440 = 24h)
# ACCESS_TOKEN_EXPIRE_MINUTES=1440

# -----------------------------------------------------------------------------
# Caddy - HTTPS (production)
# -----------------------------------------------------------------------------

# Domaine pour le certificat HTTPS
DOMAIN=sync.example.com

# -----------------------------------------------------------------------------
# Challenge DNS-01 (si ports 80/443 non accessibles)
# -----------------------------------------------------------------------------
# Décommenter et remplir si vous utilisez le challenge DNS-01 au lieu de HTTP-01
# Voir README.md pour plus de détails

# -- OVH --
# Créer les credentials sur : https://api.ovh.com/createToken
# Droits nécessaires : GET/POST/PUT/DELETE /domain/zone/*
# OVH_ENDPOINT=ovh-eu
# OVH_APPLICATION_KEY=your_application_key
# OVH_APPLICATION_SECRET=your_application_secret
# OVH_CONSUMER_KEY=your_consumer_key

# -- Cloudflare (alternative) --
# CLOUDFLARE_API_TOKEN=your_api_token

# -- Autres providers --
# Voir https://github.com/caddy-dns pour la liste complète

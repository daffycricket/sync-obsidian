# Image Caddy avec plugins OVH (DNS-01) et rate limiting
FROM caddy:2-builder AS builder

RUN xcaddy build \
    --with github.com/caddy-dns/ovh \
    --with github.com/mholt/caddy-ratelimit

FROM caddy:2-alpine

COPY --from=builder /usr/bin/caddy /usr/bin/caddy
